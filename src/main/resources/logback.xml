<configuration>
    <appender name="logAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <FileNamePattern>logs/log_%d{yyyy-MM-dd}.log</FileNamePattern>

            <!-- keep 30 days' worth of history -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %X{reportId} %X{user} %X{role} %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the messageType
              ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %X{reportId} %X{user} %X{role} %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.gordeev.movieland" level="TRACE"/>
    <root level="info">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="logAppender"/>
    </root>
</configuration>